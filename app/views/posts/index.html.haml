.page-header
  %h1 Posts

- if current_user.class == 'AdminUser'.constantize
  .btn= link_to 'New post', new_post_path

- @posts.each do |post|
  .blog-post
    .blog-title= link_to post.title, post
    %hr
