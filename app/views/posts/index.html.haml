.page-header
  %h1 Posts

- if current_user
  - if current_user.is_admin?
    = link_to 'New post', new_post_path, class: 'btn'
    = link_to 'Deleted posts', destroyed_path, class: 'btn'

- @posts.each do |post|
  .blog-post
    .blog-title= link_to post.title, post
    %hr
