.page-header
  %h1 Sign Up

= simple_form_for @user, url: sign_up_path do |f|
  = f.input :email
  = f.input :password
  = f.input :password_confirmation
  = f.select :type, ['AdminUser', 'ReaderUser']

  = f.submit 'Sign up', class: 'btn'
